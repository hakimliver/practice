package other_project.basic;

public class hakimMethod {

     int myMethod(int x, int y) {// this is a method(constructor) with 2 parameters
        return x + y;//The void keyword, used in the examples above,
         // indicates that the method should not return a value.
         // If you want the method to return a value,
         // you can use a primitive data type
         // (such as int, char, etc.)
         // instead of void, and use the return keyword inside the method:

    }


    public static void main(String[] args) {
        hakimMethod ss=new hakimMethod();// you can also use directly system.out.print(ss.hakimmethod(x 3,y 4))
        int z= ss.myMethod(4,2);
        System.out.println(z);




    }










}
